# Codex 작업 공간 앱: 설계와 조사

Windows에서 수십 개 Codex CLI 세션을 운용하기 위한 개인용 앱의 설계 작업 공간이다. 구현 저장소는 아직 아니며, 독립 설계 검수 통과와 실제 기능 검증을 구분한다.

## 현재 문서

- [합의된 요구사항과 판단 근거](2026-09-13_design-codex-workspace-report.md)
- [채택한 실행 아키텍처와 독립 검수 결과](2026-09-13_runtime-architecture.md)
- [토폴로지 Mermaid 원본](runtime-topology.mmd)
- [Codex 설정·인증·대화 재개 분석](2026-09-13_codex-config-auth-analysis.md)
- [백그라운드 실행과 업데이트 분석](2026-09-13_background-update-analysis.md)
- [최초 제품·상태 감지 조사](analysis.ko.md): 이후 독립 앱 개발을 선택했으므로 제품 방향은 현재 결정 문서를 따른다.

## Git에 보존하는 범위

문서, 소스 기준 커밋 목록, 검증 결과, 분석에 직접 사용한 설치 CLI의 JSON 스키마 3개를 보존한다. 참고 저장소는 중첩 저장소나 submodule로 추가하지 않는다. 원시 probe 폴더·개인 Codex 홈·인증정보도 추가하지 않는다.

문서의 `refs/`, 기존 클론 디렉터리, `probe-*`와 다른 작업 폴더를 가리키는 링크는 현재 컴퓨터에 보존된 로컬 근거다. 이 저장소만 새로 clone하면 해당 자료는 함께 내려오지 않는다. 참고 소스의 원격·커밋은 [기존 목록](repository-manifest.json)과 [후속 refs 목록](refs-manifest.json)을 따른다. 과거 probe는 기존 검증 기록이지 이 저장소만으로 재실행할 수 있는 테스트 패키지가 아니다.

전체 생성 스키마도 로컬에 남지만 분석에 필요한 부분만 커밋한다. 확인한 CLI 명령은 `codex app-server generate-json-schema --experimental --out <출력 폴더>`다. 재생성 시 CLI 버전을 확인한다. 버전 문자열이 같아도 과거 기록의 Git 태그와 커밋이 일치한다고 가정하지 않는다.

## 다음 작업: G1 최소 실험

화면 앱 전체 구현 전에 임시 환경·합성 인증·로컬 모의 서비스로 CLI와 인증 경계를 검증한다.

1. 같은 프로젝트 환경의 두 세션을 서로 다른 계정 바인딩으로 실행한다.
2. 세션 하나의 바인딩 변경이 다른 세션에 영향을 주지 않고, 스킬·MCP·지침·권한 설정이 유지되는지 확인한다.
3. 인증 갱신·연결 단절·갱신 결과 저장 전 충돌에서 중복 실행과 무한 재시도가 없는지 확인한다.
4. 실제 요청의 계정과 앱 표시가 일치하는지 확인하고 프록시·외부 인증 중 채택 가능한 경로를 결정한다.

모의 시험은 실제 계정·서비스의 호환성 증거를 대체하지 않는다. G1 실패 시 구조를 수정한다. G2 다중 세션·터미널, G3 수명주기, G4 실제 설치 업데이트 시험은 아키텍처 문서에 정의되어 있다.
