# Codex 작업 공간 앱: 요구사항과 결정 기록

작성일: 2026-09-13 (한국 시간)

범위: 이 대화에서 합의한 제품 방향, 판단 근거, 독립 UX 검수 결과

상태: 요구사항 인터뷰 진행 중. 구현 명세나 개발 완료 보고서가 아니다.

## 1. 제품의 목적과 현재 결론

Windows에서 수십 개의 기존 Codex CLI 세션을 운용하면서, 지금 확인해야 할 세션을 쉽게 찾고 필요한 터미널들을 함께 볼 수 있는 개인용 앱을 만든다. 계정 선택·사용량 확인·장애 복구는 기본적으로 앱이 처리한다. 사용자가 원할 때만 동작을 조정한다.

프로젝트별로 터미널을 묶어 보는 기존 습관을 지원하되, 프로젝트 전환이 잦다는 문제를 해결하기 위해 서로 다른 프로젝트의 터미널도 한 작업 화면에 함께 배치할 수 있는 방향을 채택한다. **프로젝트 분류와 현재 화면에 배치하는 터미널 구성을 반드시 일치시킬 필요는 없다.**

여러 배치를 오가는 작업 탭을 지원하고 같은 세션을 여러 작업 탭에 배치할 수 있게 한다. 그룹 탐색과 사이드바의 구체적 형태는 확정하지 않았다. 자유로운 배치보다 더 중요한 기준은 화면에 보이지 않는 세션까지 포함한 상태 가시성이다.

이 문서는 앞선 [기술 조사](analysis.ko.md)를 대체하지 않는다. 다만 그 보고서의 wmux 포크 추천 이후, 사용자는 독립 앱 개발을 선택했다. 제품 방향은 이 결정 기록을 기준으로 읽는다. 기술 조사에서 확인한 범위와 한계는 원문을 따른다.

## 2. 기록을 읽는 기준

| 구분 | 의미 |
|---|---|
| 합의 | 사용자가 직접 요청하거나 제안에 동의한 제품 동작 |
| 조건부 목표 | 원하는 방향은 분명하지만 기술적으로 가능한 범위에서 구현 |
| 설계 제안 | 추천 또는 검수 의견이며 세부 동작은 아직 확정하지 않음 |
| 미결정 | 후속 인터뷰나 기술 검증이 필요한 사항 |

기능에 동의했다는 사실과 현재 Codex에서 구현 가능하다는 사실은 다르다. 특히 인증 이전, 턴 도중 복구, 모델 복원은 실제 통합 검증 없이 보장하지 않는다.

## 3. 기본 범위와 계정 관리

| ID | 구분 | 결정 | 판단 근거 |
|---|---|---|---|
| D01 | 합의 | 독립 Windows 앱을 만들며 첫 버전은 개인용이다. Codex만 지원한다. | 사용자 자신의 다중 터미널·다중 계정 불편을 우선 해결한다. 팀 공유 계정이나 권한 관리는 첫 범위에 포함하지 않는다. |
| D13 | 합의 | 첫 버전의 대상 실행 환경은 Windows PowerShell·CMD이며 macOS는 장기 지원 대상이다. | 사용자는 현재 PowerShell·CMD에서 Codex를 실행한다. WSL 지원은 첫 버전 요구로 확정하지 않았고 macOS 구현도 당장 범위에 넣지 않는다. |
| D14 | 조건부 승인 충족: 설계 채택 | 화면 앱과 OS 사용자당 하나의 독립 백그라운드 Runtime으로 책임을 나눈다. | 사용자가 독립 검수 후 채택을 승인했고 r2 설계가 PASS를 받았다. Runtime은 터미널·인증·라우팅·usage 조회·복구를 소유한다. 구현은 G1 최소 실험부터 하며 무중단 업데이트 출시는 별도 시험을 통과해야 한다. |
| D02 | 합의 | 기존 Codex CLI/TUI를 유지한다. | 첫 버전에서 별도 채팅 UI로 사용 방식을 바꾸지 않는다. |
| D03 | 합의 | 기본 설정과 계정 선택은 자동화한다. | 복잡한 설정 없이 사용자가 생각하는 최적의 동작을 제공해야 한다. 최적화의 구체적 우선순위는 아래 결정을 따른다. |
| D04 | 합의 | Orca·OpenCodex에서 인증정보를 가져온 뒤 새 앱이 독립적으로 관리한다. | 기존 앱을 계속 실행하거나 원본 인증 파일을 유지해야 하는 종속성을 원하지 않는다. |
| D05 | 합의 | 기존 앱과의 인증 동기화·동시 사용 보장은 범위에서 제외한다. | 사용자는 독립 이전만 필요하다고 명시했다. 같은 계정 인증을 여러 앱이 함께 갱신하는 운용을 설계할 필요가 없다. |
| D06 | 합의 | 터미널마다 서로 다른 계정을 사용할 수 있고 연결된 계정을 확인할 수 있어야 한다. 전체 계정 사용량도 함께 모니터링한다. | 여러 계정을 수동 전환하며 사용했던 부담을 줄인다. |
| D11 | 합의 | 계정 전환 때문에 같은 세션의 스킬·도구 구성이 달라져서는 안 된다. | 계정은 인증·용량 선택의 대상이며 작업 환경 선택의 대상이 아니다. 프로젝트별 차이와 사용자가 의도한 설정은 존중한다. |
| D12 | 합의 | 사용자·프로젝트 환경을 유지하고 계정 인증과 요청 라우팅을 별도 관리한다. | 작업 환경의 연속성과 계정 자동화를 함께 달성하기 위한 현재 구조적 방향이다. 부작용이 확인되면 해결책을 검토하고 필요하면 구조 자체도 변경한다. |

D11의 기술 근거와 남은 제약은 [Codex 설정·인증 분리 분석](2026-09-13_codex-config-auth-analysis.md)에 기록했다. 외부 인증 API가 존재하지만 내부용·불안정이라는 사실을 포함하며, 프록시나 인증 공급 방식은 아직 확정하지 않았다.

D13에 따른 설계 기준: 세션·작업 탭·계정 선택·페이스메이킹 같은 공통 로직과 운영체제별 터미널 실행·경로 처리·인증 저장·프로세스 수명주기 처리를 분리한다. 이는 macOS 후속 지원을 위한 설계 제안이며 구체적 프레임워크나 모든 플랫폼의 동시 구현을 확정한 것은 아니다.

기존 [Orca 가져오기 코드](../../projects/opencodex-orca-import/src/codex/orca-import.ts)와 [인증 파서](../../projects/opencodex-orca-import/src/codex/orca-auth-source.ts)는 참고 자료다. 대화 중 확인한 구현은 refresh token을 비우고 원본 경로를 유지하는 방식이므로, 그대로 재사용하면 D04를 만족하지 않는다. [계정 저장·갱신 코드](../../projects/opencodex-orca-import/src/codex/account-store.ts)는 독립 갱신 구현을 검토할 자료다. 이 링크들은 변경될 수 있는 로컬 작업 트리를 가리킨다.

독립 이전의 검증 기준은 원본 앱과 인증 파일을 참조하지 않고 가져온 계정의 인증 갱신까지 성공하는 것이다. 현재 실제 계정으로 이 기준을 검증한 것은 아니다. 원본 인증정보 삭제는 독립 이전의 필수 동작으로 합의하지 않았다.

## 4. 계정 전환과 페이스메이킹

| ID | 구분 | 결정 | 판단 근거 |
|---|---|---|---|
| D07 | 합의 | 정상 진행 중인 세션은 다른 계정의 리셋이 임박했다는 이유만으로 강제 전환하지 않는다. | 사용량 최적화가 세션의 안정성과 연속성을 임의로 침해하지 않아야 한다. |
| D08 | 합의 | 리셋 전 용량 손실을 줄이기 위한 전환 제안은 비차단 방식으로, 예측상 필요한 시점보다 여유를 두고 제공한다. | 손실 직전에 알려주면 실제로 남은 용량을 활용할 시간이 부족하다. |
| D09 | 합의 | 사용자가 허용한 경우에는 용량 활용을 위한 선제적 자동 전환을 지원하는 방향으로 한다. 정상 세션의 전환은 턴 사이에서 한다. | 적극적인 용량 활용을 원하는 선택권과 기본 세션 유지 원칙을 함께 제공한다. |
| D10 | 합의 | 특정 계정으로 한 번 전환하는 것과 그 계정에 고정하는 것을 구분한다. | 수동으로 계정을 골랐다는 이유만으로 이후 자동 복구까지 막으면 의도와 다르다. |

페이스메이킹은 계정별·전체 사용량, 남은 시간과 예상 소비를 참고한다. 제안의 정확한 계산식, 버퍼, 알림 빈도, 계정 선택 점수는 미결정이다. 캐시·현재 계정 유지를 우선하는 선택권도 요구에 포함한다. 사용량의 정확한 측정 가능 범위와 갱신 주기는 별도 검증한다.

후속 명확화: **페이스메이킹은 사용자가 사용량 전망을 보고 스스로 작업 속도를 조절하도록 돕는 기능이다.** 앱이 예측에 따라 사용량을 강제로 제한하거나 작업을 중단하지 않는다. 사용자는 속도를 조절함으로써 사람이 개입해 결과를 견고하게 검수할 시간을 확보하려 한다. 남은 용량 소진 자체를 사용자의 최종 목표로 취급하지 않는다. 이는 실제 서비스 한도에 도달했을 때의 자동 대기·복구와는 별개다.

페이스메이킹의 전망 제공과 계정 선택·전환 최적화는 서로 독립된 기능이다. 장단점 설명 후 사용자는 새 세션은 자동 선택하고 정상 세션은 유지하며 막히면 자동 복구하는 기본 방향에 동의했다. 정상 세션의 용량 활용 목적 전환은 기본적으로 조기 제안만 한다. 정확한 계정 선택 점수는 미결정이며, 최적화가 새 작업 생성이나 병렬 작업 수 증가를 뜻하지 않는다.

**첫 버전의 전망은 리셋 시각 기준으로만 제공한다.** 사용자가 작업 종료 시각이나 별도의 계획 기간을 입력하는 기능은 첫 버전 범위에서 제외한다. 소비 추세와 예상 소진·리셋 전 잔여량을 계정별·전체 관점에서 보여주는 방향이며, 여러 사용량 한도와 서로 다른 리셋 주기를 어떻게 표현하고 집계할지는 관측 데이터에 맞춰 검증한다. 전망은 추정치로 표시하고 강제 제한에는 사용하지 않는다.

후속 범위 명확화: 병렬 작업이 많지만 세션별 별도 작업 폴더·worktree를 강제 제공하지 않는다. 사용자가 작업 폴더를 선택하며 같은 폴더를 여러 세션에서 사용할 수 있는 방향이다. 앱의 계정·세션 관리와 프로젝트 작업 공간 분리는 별도 책임으로 다룬다.

## 5. 자동 복구와 고정의 의미

| 상황 | 합의된 동작 |
|---|---|
| 고정하지 않은 세션이 턴 중 한도·인증 문제 등으로 진행 불가 | 다른 계정으로 자동 복구를 허용한다. 정상 세션을 최적화 목적으로 전환하는 것과 구분한다. |
| 고정한 계정으로 진행 불가 | 고정을 유지하고 기다린다. 막힌 이유와 알 수 있는 예상 재개 시각을 표시한다. 다른 세션은 계속 진행한다. |
| 사용자가 다른 계정으로 재개 선택 | 기존 계정 고정을 해제하고 자동 선택 상태에서 다른 계정으로 재개한다. 이전 계정으로 자동 복귀하는 임시 전환 개념을 두지 않는다. |
| 사용 가능한 계정이 없음 | 자동 대기 후 사용할 수 있게 되면 재개한다. 대기 이유·예상 재개 시각·자동 재개 취소 수단을 제공한다. 고정한 세션은 해당 계정만 기다린다. |

전환이 고정을 해제한다는 결과는 실행 전에 직관적으로 보여야 한다. 예를 들어 버튼을 **‘고정 해제 후 다른 계정으로 재개’**로 표시하고, 이후 패널에서도 새 계정과 고정 해제 상태를 확인할 수 있게 한다. 문구 자체는 최종 UI 작성 때 다듬을 수 있다.

복구 시 이미 실행된 명령이나 파일 수정을 중복 실행하지 않고 이어갈 수 있는지 검증해야 한다. 안전한 복구가 불가능하면 해당 세션에 사용자 개입이 필요함을 표시하는 것이 현재 제안이다. 어떤 오류를 얼마 동안 재시도할지, 복구의 안전성을 어떻게 판정할지는 아직 확정하지 않았다.

## 6. 모델 유지와 복원

기본 방향은 사용자가 선택한 모델을 유지하면서 다른 계정으로 재시도하는 것이다. 앱이 임의로 모델을 낮추는 것은 기본 동작으로 두지 않는다.

설정에서 허용한 경우에만 더 낮은 모델로 계속 진행할 수 있는 방향을 검토한다. 앱이 일시적으로 낮춘 모델은 사용자가 직접 변경한 모델과 구분하고, 가능해지면 원래 모델로 돌아오는 기능도 조건부 목표다.

이 항목들은 사용자가 기술적으로 가능하면 구현하고 불가능하면 반드시 구현할 필요는 없다고 명시했다. 모델 복원과 계정 잔여 용량 중 무엇을 우선할지, 실제로 계정 변경만으로 모델 가용성을 회복할 수 있는 상황은 무엇인지, CLI 설정을 어떻게 존중할지는 미결정이다.

## 7. 수십 개 세션을 보는 화면

### 사용자의 실제 작업 방식

사용자는 수십 개 터미널을 사용하므로 모든 본문을 한 화면에 표시하기 어렵다고 설명했다. 프로젝트별로 묶어 보는 편이지만 전환이 너무 잦아, 필요한 터미널들을 프로젝트 구분 없이 모아 보는 방식에도 동의했다.

따라서 프로젝트별 탐색과 여러 프로젝트를 함께 보는 작업 화면을 모두 지원하는 방향으로 한다. 후속 대화에서는 프로젝트별 세션 분류와 배치를 담는 작업 탭을 구분하는 방향으로 이해를 맞췄다. 같은 세션의 다중 작업 탭 배치는 허용하되, 다중 그룹 소속 여부까지 합의한 것은 아니다.

### 작업 탭과 세션의 관계: 후속 합의

- 한 화면으로 모든 배치를 담을 수 없으므로 여러 작업 탭을 전환할 수 있어야 한다. 각 탭은 서로 다른 터미널 개수와 배치를 가질 수 있다.
- 같은 세션을 여러 작업 탭에 배치할 수 있다. 사용자는 이를 막을 필요가 없다고 동의했다.
- 여러 탭에 배치해도 실행 중인 세션은 하나이며, 어느 탭에서 접근하든 같은 대화로 이어진다. 세션 복제나 별도 계정 배정을 뜻하지 않는다.
- 예를 들어 A 구현 세션을 ‘A 프로젝트’ 탭과 ‘지금 집중할 작업’ 탭에 함께 배치할 수 있다. 배치는 탭마다 다르지만 실행 상태와 연결 계정은 해당 세션의 상태를 따른다.
- 프로젝트를 열 때 기본 작업 탭을 자동 생성하는 방식, 탭별 배지, 배치 저장·복원의 세부 동작은 설계 제안이다.
- 후속 합의: 패널·작업 탭 닫기는 배치만 제거한다. 세션은 계속 실행되고 전체 세션 목록에 남는다. 목록에서 세션을 삭제할 때는 실행 종료를 명확히 안내하고 확인을 받은 뒤 모든 탭에서 제거한다. 대화 기록과 프로젝트 파일은 보존한다.

### 대안 비교와 판단

| 대안 | 장점 | 한계 | 현재 판단 |
|---|---|---|---|
| 그룹별 탭과 분할 화면 | 프로젝트 단위로 익숙하게 전환할 수 있다. | 그룹 밖의 필요한 터미널이 숨고 그룹 간 비교가 어렵다. | 탐색 방식 후보로 유지하되 작업 화면의 유일한 구조로 제한하지 않는다. |
| 전체 세션 카드 보드 | 세션별 상태를 비교하기 쉽다. | 수십 개 카드가 정보 과밀을 일으키고 본문 공간을 줄일 수 있다. | 전체 현황을 보는 보조 화면 후보이며 기본 화면으로 확정하지 않는다. |
| 전체 세션 목록·그룹 필터·자유로운 분할 화면 | 분류와 무관하게 필요한 터미널을 함께 보고 전체 상태를 살필 수 있다. | 목록 선택, 필터 변경, 화면 배치의 의미가 불명확하면 복잡해진다. | 현재 추천 방향. 사용자는 프로젝트를 넘는 배치의 효용에 동의했으며 세부 UI는 미확정이다. |

독립 검수의 핵심 결론은 **그룹은 탐색에 적합하지만 상태 감시까지 그룹에 맡기면 중요한 세션이 다시 숨는다는 것**이다. 위 비교는 검수 당시 대안이며, 후속 합의로 여러 작업 탭과 동일 세션의 다중 탭 배치를 추가했다. 배치 저장의 세부 동작, 목록의 위치, 전체 현황 화면은 설계 제안으로 남긴다.

### 상태 표시에서 지켜야 할 설계 기준

아래는 독립 검수를 반영한 설계 제안이며, 개별 표시 방식까지 사용자와 합의한 것은 아니다.

- 사용자 승인·응답이 필요한 대기와 자동 복구·한도 리셋 대기를 구분한다.
- 실행 상태와 읽지 않은 새 응답을 분리한다. 한 턴 종료를 프로젝트나 작업 전체 완료로 표시하지 않는다.
- 선택하지 않은 그룹에서도 확인 필요 상황을 알 수 있게 한다. 그룹 집계에서 해당 세션으로 바로 이동할 수 있어야 한다.
- 색상만 사용하지 않고 아이콘·문구·숫자를 함께 제공한다.
- 상태 변화로 목록 순서나 키보드 포커스를 임의로 바꾸지 않는 것을 기본안으로 한다.
- 같은 계정 장애로 여러 세션이 막히면 원인을 묶어 보여주되 영향을 받은 세션에 접근할 수 있게 한다.
- 관측 연결이 끊기면 이전 정상 상태를 확정적으로 유지하지 않고 ‘상태 확인 불가’를 표시한다.
- 계정 고정과 화면 배치 유지는 용어와 아이콘을 구분한다.

## 8. 근거와 결정의 연결

| 근거 | 관찰 또는 발언 | 해석·판단 | 반영 위치 |
|---|---|---|---|
| E01: 사용자 대화 | “기존 cli 유지”, “첫버전은 개인용이면 충분” | 기존 작업 방식을 유지하는 개인용 독립 앱 | D01–D02 |
| E02: 사용자 대화 | “아뇨 독립이전만 합니다” | 원본 앱과 동기화하는 연결형 가져오기 대신 독립 인증 관리 | D04–D05 |
| E03: 사용자 대화 | 정상 세션을 리셋 임박 때문에 강제 전환하는 것은 부적절하다고 수정 | 용량 소진보다 기본 세션 유지가 우선. 비차단 조기 제안과 명시적 허용으로 보완 | D07–D09 |
| E04: 사용자 대화 | 계정 고정과 전환을 구분하고, 다른 계정으로 전환은 임시 복귀 개념이 아니라고 명시 | 전환 시 고정 해제 결과를 명확히 표현 | D10, 5절 |
| E05: 사용자 대화 | “네 자동복구 허용”, 사용 가능 계정 부재 시 자동 대기에 동의 | 장애 복구와 정상 세션 최적화를 다른 정책으로 취급 | 5절 |
| E06: 독립 에이전트 검수 | 그룹 중심 상태 집계는 숨겨진 세션을 다시 만들 수 있음 | 전역 상태 가시성과 실제 작업 배치를 분리할 필요 | 7절 |
| E07: 사용자 최신 발언 | “한 프로젝트의 터미널들을 묶어 보는편… 전환이 너무 잦으므로 한번에 필요한 터미널들을 다 모아서 보는것도 좋을 것” | 프로젝트별 탐색을 유지하면서 프로젝트를 넘는 배치를 지원 | 1절, 7절 |
| E08: 기존 기술 조사 | 상태 경로 조사·모의 실행 시험과 실제 다중 계정 통합 시험의 범위가 다름 | UI 합의만으로 복구·인증·상태 감지 구현을 보장하지 않음 | 2절, 9절 |

E01–E07은 이 대화와 사용자가 요청한 독립 에이전트 `workspace_ux_review`의 검수 결과를 근거로 한다. 제품 선호에 대한 기록이며 외부 제품의 현재 기능을 입증하는 자료가 아니다. E08의 상세 근거는 [조사 보고서](analysis.ko.md), [저장소 기준 커밋](repository-manifest.json), [기존 검증 결과](verification-summary.json)에 있다. 이 문서 작성 시 기존 실행 시험을 다시 수행하지 않았다.

## 9. 다음에 결정하거나 검증할 사항

창 닫기 후 백그라운드 유지에 사용자는 긍정적이다. 업데이트 중 유지도 유용하지만 단점과 근본적 해결 구조를 확인한 뒤 결정하며, 적절한 해결책이 없으면 당장은 구현하지 않겠다고 명시했다. [Orca 백그라운드·업데이트 분석](2026-09-13_background-update-analysis.md)은 창 닫기 유지와 무중단 UI 업데이트·실행부 업데이트를 구분한다. 현재 추천은 창 닫기 시 트레이 유지부터 제공하고 무중단 업데이트는 검증 전까지 보류하는 것이며, 이를 사용자 최종 합의로 처리하지 않는다.

후속 결정 D14: 사용자의 독립 검수 요청에 따라 [실행 토폴로지 r2](2026-09-13_runtime-architecture.md)를 검수·수정하고 채택했다. 위 문단의 트레이 유지 최소안은 당시 추천 기록이다. 현재는 UI/Runtime 분리를 설계 기준으로 하며, 호환되는 UI 업데이트만 실행 유지 대상으로 검증하고 Runtime 교체는 작업 종료까지 연기한다. 프로세스 분리 자체를 업데이트 기능 완료로 보지 않는다.

직전 인터뷰의 추가 요구: 잔여량은 앱의 요청량 합산이 아니라 계정 usage/status 조회로 갱신한다. 전망은 조회 상태의 추세를 이용하는 별도 추정이다. 읽음과 검수의 표시 방식은 사용자에게 설계 판단을 위임받았으며, 현재 제안은 새 응답 표시와 명시적 검수 표시를 분리하는 것이다. 자동으로 검수 대상을 판별하는 방법과 구체적인 시각 표현은 미확정이다.

| 항목 | 남은 결정 또는 검증 |
|---|---|
| 탐색과 배치 | 그룹 목록 선택과 작업 탭 전환의 UI 구분, 배치 저장·복원, 여러 탭에 있는 세션으로 이동할 때 대상 탭 선택 |
| 확인 필요와 알림 | 전역 표시 위치, 새 응답을 읽었다고 판단하는 기준, 알림 반복·음소거, 포커스 보존 |
| 앱 수명주기 | 창 닫기·앱 종료·PC 재시작 시 세션과 대기 작업을 어떻게 처리할지 |
| 인증 독립 이전 | 인증 형식별 호환성, 갱신 성공, 실패 계정 재로그인 흐름, 안전한 저장 방식 |
| 계정 최적화 | 계정 선택 순위, 소비 예측, 리셋 전 제안 버퍼, 캐시 유지 우선 옵션 |
| 자동 복구 | 복구 가능한 오류와 중복 실행 위험, 재시도 횟수·간격, 취소와 사용자 개입 기준 |
| 모델 정책 | 사용자 설정 존중, 임시 하향과 복원 가능성, 용량 최적화와의 우선순위 |
| 기술 구조 | 환경과 인증·라우팅의 분리는 현재 합의 방향이다. 프록시 필요 여부, CLI 상태 연결 방식, 터미널·UI 기술 스택은 미확정이며 부작용에 따라 구조도 재검토할 수 있다. |
| 실제 사용성 | 수십 개 세션의 성능, 한글 입력, 키보드 탐색, 화면 크기별 정보 밀도 |

후속 인터뷰에서는 제품 사용 방식에 큰 영향을 주는 미결정 사항을 다룬다. 사용자는 실행 종료 확인 같은 통상적인 동작을 반복해서 질문하지 말고 판단해 반영해 달라고 요청했다. 소스에서 답을 확인할 수 있는 질문은 조사로 해결한다. 간단한 화면 시안에서는 프로젝트별 탐색과 여러 프로젝트의 동시 작업이 모두 자연스러운지 확인한다.

## 10. 문서 검증 범위

작성 후 합의와 제안의 구분, 최신 발언 반영, 이전 결정과의 충돌 여부를 대조하고 상대 경로 링크의 존재를 확인한다. 이 검증은 요구사항 문서의 정합성 검증이며 기능 구현·실계정 동작·성능 검증이 아니다.
