# 구현 현황

최종 갱신: 2026-09-13. 대상: Windows 개발판 0.1.1.

기존 Codex TUI를 Electron 패널에 연결하는 앱 구현이 추가되었습니다. 이전 설계 검수 통과는 제품 검증 완료를 뜻하지 않습니다. 이 문서는 구현된 동작과 아직 검증하거나 확장해야 할 범위를 구분합니다.

## 구현 범위

| 영역 | 현재 구현 | 남은 범위·제약 |
| --- | --- | --- |
| 터미널 | 세션별 Codex app-server와 원격 TUI, xterm 화면 | Codex CLI 0.154.0만 허용. 외부 인증 API는 upstream이 내부용·불안정으로 명시한 인터페이스 |
| 작업 공간 | 프로젝트 폴더별 세션 목록, 탭별 배치, 열 너비·패널 순서, 동일 세션의 여러 패널 | 임의 위치의 플로팅 창·도킹, 사용자 정의 그룹은 미구현 |
| 상태 | 프로토콜 기반 진행·승인·입력·대기·오류 표시, 숨겨진 작업 표시, 읽음과 검수 구분 | 프로젝트 전체 완료를 자동 판단하지 않음 |
| 계정 | Orca 표준 경로 자동 인식·독립 이전, DPAPI 저장, 갱신 직렬화, Codex 확인 후 적용 계정 표시·고정·전환, CLI 인증 저장소의 메모리 격리 | 실제 구독 계정으로 갱신 전체 흐름 미검증. 원본 앱과 계속 동기화하지 않음 |
| 사용량 | 전체 등록 계정의 주기·수동 조회, 리셋 구간별 관측 이력과 전망 | 전망은 추정치. 계정 간 절대 용량 합계와 장기 일정 전망 없음 |
| 자동 복구 | 실행 전 실패로 확인된 순수 텍스트 턴에 제한, 대기·재시도 간격, 입력·취소·고정 확인 | 도구·응답 이후의 실패는 자동 재전송하지 않음. 모델 자동 하향·복원 없음 |
| 수명주기 | UI와 실행부 분리, 창을 닫아도 세션 유지, 다시 연결, 명시적 세션 종료 | OS 재시작·실행부 충돌 후 프로세스 생존 보장 없음 |
| 배포 | Windows 압축 해제 실행 형태의 빌드 설정 | 자동 업데이트 비활성화. 실행부의 라이브 이전이나 실제 구버전→신버전 업데이트 보장 없음 |
| 대화 재개 | 대화 ID를 지정하여 기존 저장소의 대화 재개 | Orca 대화 저장소를 가져오는 별도 마이그레이션 화면은 없음 |

## 검증 기록

아래는 2026-09-13 실행 결과입니다. 실제 구독 계정은 자동 테스트에 사용하지 않았습니다.

| 검증 | 확인하는 범위 | 현재 기록 |
| --- | --- | --- |
| `npm test` | Orca 자동 인식, 계정 저장·갱신, 적용 계정 확인, 상태 처리, 실행부 명령과 경합의 자동 테스트 | 34 개 통과. 실제 Windows DPAPI 검사 포함 |
| `npm run probe:auth` | 실제 설치 CLI와 합성 계정·로컬 모의 서비스로 인증 격리·전환·갱신, TUI 입력·제한적 자동 복구, 로그아웃 시 공용 인증파일 보존 검증 | [PASS, 07:27:04 UTC](experiments/g1-results.json). 실제 구독·사용량 제한 시험은 아님 |
| `npm run test:desktop` | 패키지 Electron 화면과 데스크톱 연결 검사 | 합성 Orca 계정 자동 인식, 창 닫힘, 빈 실행부 유지, 즉시 재실행·저장된 탭 복구, 이전 UI 프로세스의 최종 종료 통과. 실행 중인 실제 Codex 작업의 유지 시험은 아님 |
| `npm run test:workspace` | 합성 다중 세션의 화면·배치 동작 | 30 개 세션, 패널의 적용 계정 표시, 숨은 탭 상태, 읽음·검수 분리, 미러 입력 소유권과 동일 화면 크기, 배치 저장·재로드 통과. 실제 Codex 30 개 부하 시험은 아님 |
| `node scripts/runtime-smoke.mjs` | 실행부 동시 시작과 종료 | 동시에 시작한 실행부 2 개 중 1 개만 쓰기 권한을 확보하고, 명시적 종료까지 통과 |
| `npm run dist` | 화면 빌드·Windows 실행 폴더·ZIP 생성 | 통과. `release/win-unpacked`와 `release/Codex Workspace-0.1.1-win.zip` 생성. 패키지 생성과 업데이트 검증은 별개 |

`node scripts/native-smoke.mjs`도 통과했습니다. 배포 폴더의 Electron 실행 파일이 ASAR 안의 node-pty를 읽고 실제 CMD 출력을 반환하는 것을 확인했습니다(`PACKAGED_PTY_OK`). 실제 패키지 업데이트 시험은 아닙니다.

Windows에서 창이 닫힌 뒤 Electron 프로세스 정리가 약 20초 걸린 사례가 있습니다. 이 동안에도 앱을 즉시 다시 열어 연결하는 시험은 통과했습니다.

실제 계정의 서비스 응답, 계정에 연결된 도구와 조직 정책, 장시간 다중 세션 부하는 별도로 확인해야 합니다. 사용법은 [README](README.md), 구현 인터페이스는 [구현 계약](IMPLEMENTATION_CONTRACT.md), 장기 검증 단계는 [아키텍처 문서](2026-09-13_runtime-architecture.md)를 참고하세요.
